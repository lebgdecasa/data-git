# Copy this file to .env and fill in your endpoint.
# The application form POSTs its JSON payload to this URL.
# A Google Apps Script web-app URL backed by a Google Sheet works well.
# Leave it empty to preview the flow in dev (submissions are logged to the
# browser console and the success state still shows).
VITE_FORM_ENDPOINT=
